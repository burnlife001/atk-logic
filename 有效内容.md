# 2026-06-03 会话有效内容

## 1. CLI 采集 UART 错误率 50%+ 根因与修复

### 根因
- **`selectHzIndex = 0` 硬编码** → FPGA PLL 配置与软件元数据不一致，产生亚稳态采样
- **`findChild` 返回 demo SessionController** → 应 `findChildren` 遍历找 `sessionType==1`

### 修复
- `pv/thread/thread_cmd.cpp`: 删除自建 SessionController 逻辑，改为纯代理模式
  - `findChildren<SessionController*>()` 遍历找 `sessionType == 1` 的设备会话
  - 读取 QML SSettings（`Setting` 类型）的 `settingData`/`channelsSet`/`isInstantly`
  - 仅覆盖 `setTime`（duration），调用 `controller.start()` — 与 GUI 按钮完全一致
- CLI 默认参数改为匹配 GUI：RLE=false, threshold=3.3V, sample-rate-hz=100MHz

### 验证
- 方波：7-10 edges/s（1Hz 0.5s/0.5s 方波精确捕获）
- 边缘密度从 2830 edges/ms 降至 0.01 edges/ms

## 2. 文件清理

删除了 18 个文件：
- 9 个孤立 `_*.py` 辅助脚本（无引用）
- 6 个 `_test_*.py` 测试脚本
- `_debug.py`, `10}ns`, `todolist.md`
- 4 个 `uart_*.atkdl` 测试采集文件

保留 6 个 `_*.py`：`_bootstrap`（import 引用）、`_analyze`/`_check_ch`/`_dump_edges`/`_dump_frames`（SKILL 引用）、`__init__`

## 3. Code Discovery 配置

- **codegraph CLI**: `D:/Programs/nodejs/node_global/codegraph`，项目已索引
- `.claude/CLAUDE.md` 已更新，明确 CLI 优先原则
- 全局 `CLAUDE.md` 已修正：codegraph CLI + codebase-memory MCP（仅 query_graph）

## 4. CmdServer 最终架构

```
CLI (Python) → TCP → CmdServer → findChildren(SessionController, sessionType==1)
                                    → parent() → findChild(Setting) → 读 settingData/channelsSet
                                    → controller.start(json)  ← 跟 GUI 按钮完全一致的调用
```

无自建 Session，无 USB 端口竞争。
